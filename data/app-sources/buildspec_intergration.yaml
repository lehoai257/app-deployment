version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  pre_build:
    commands:
      - echo Intergration testing..
  build:
    commands:
      - echo test completed on `date`
      # Run integration tests